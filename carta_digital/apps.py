from django.apps import AppConfig

class CartaDigitalConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'carta_digital'  # <-- Verifica que este nombre sea EXACTAMENTE 'carta_digital'